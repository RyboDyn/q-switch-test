docker.enabled = true

process {
  errorStrategy = 'retry'
  maxRetries    = 3
  queue = {
    def duration = task.previousTrace?.duration as nextflow.util.Duration
    task.attempt > 1 && duration > "1 m" ? "TowerForge-h0Zh75iShsiH8j1ORCtol" : "TowerForge-3VWsppf8adM9spVAQC2nK-work"
  } 
}   
