docker.enabled = true

process {
  errorStrategy = 'retry'
  maxRetries    = 3
  queue = {
    (task.attempt > 1 && task.previousTrace?.duration > 1000 * 60) ? "TowerForge-h0Zh75iShsiH8j1ORCtol" : "TowerForge-3VWsppf8adM9spVAQC2nK-work"
  } 
}   
